-----------------
Musil - TODO list
-----------------

v0.6

- check/improve speed for vector calculations
- signals: add sparkle (pvoc), pconv 
- music library: orchestrate, mp, soundtypes, granular orchestrate, symbolic composition system (notes, midi in and out, musicxml out, algorithmic gen)
- system interaction: exists(path), isfile(path), isdir(path), mkdir(path), remove(path), rename(a, b), listdir(path), or exec with return value
- regex
- IDE: shortcut for commenting/uncommenting

v0.51
- add paths for IDE or find solution  (path resolver) *
- graphic plots for IDE *
- add yield function for IDE + stop function *
- tab size = 4 *

v0.5
- first public release *


CURRENT BUGS

- does not check arg types for builtin (eg. "d" for sub and linspace)
