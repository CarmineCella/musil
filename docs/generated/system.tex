% generated by tools/gendoc.py from the doc comments in src/; do not edit
\fn{(exec "command")}{=\textgreater{} the command's standard output as a string}
\fn{(getenv "NAME")}{=\textgreater{} value or nil}
\fn{(sleep seconds)}{}
\fn{(now)}{=\textgreater{} seconds since the Unix epoch (wall clock; clock is the monotonic one)}
\fn{(cwd)}{=\textgreater{} current directory}
\fn{(ls [dir])}{=\textgreater{} list of entry names, sorted}
\fn{(mkdir path)}{=\textgreater{} creates the directory and its parents; nil}
\fn{(remove path)}{=\textgreater{} deletes a file or an empty directory; 1 if something was removed}
\fn{(stat path)}{=\textgreater{} (list size is-dir modified-seconds) or nil if it does not exist}
\fn{(read-csv path)}{=\textgreater{} table. write-csv is in system.mu.}
\fn{(read-wav path)}{=\textgreater{} (list sample-rate (list channel-vector ...)); 16-bit PCM or 32-bit float WAV}
\fn{(write-wav path sample-rate channels [bits])}{=\textgreater{} nil. channels: a vector (mono) or a list of vectors; bits 16 (default) or 32 (float)}
\fn{(udp-send host port payload [osc?])}{=\textgreater{} 1 on success, 0 on failure. With osc?=1 the payload is}
\fn{(udp-receive host port [timeout-seconds])}{=\textgreater{} the next datagram as a string, or nil on timeout/failure}
\fn{(file-lines path)}{list of lines, without the trailing newline}
\fn{(write-lines path lines)}{write a list of lines, one per line}
\fn{(file? path)  (dir? path)}{existence tests by kind}
\fn{(file-size path)}{size in bytes, or nil}
\fn{(basename path)  (dirname path)  (extension path)}{string surgery on paths}
\fn{(csv-escape s)}{quote a field if it holds a comma, a quote or a newline}
\fn{(write-csv path table)}{table is a list of rows, a row a list of cells}
\fn{(elapsed thunk)}{seconds taken by calling thunk}
\fn{(wav-duration w)}{seconds of a value returned by read-wav}
