% generated by tools/gendoc.py from the doc comments in src/; do not edit
\fn{(range n)  (range a b)  (range a b step)}{a vector of evenly spaced numbers, end excluded}
\fn{(seed n)}{seed the random generator so that rand repeats}
\fn{(rand)}{a number in [0, 1); (rand n) a vector of n of them}
\fn{(sort v)}{the elements of a vector in ascending order}
\fn{(reverse x)}{a list, vector or string reversed}
\fn{(slice x start [count])}{part of a list, vector or string; negative start counts from the end}
\fn{(find x v)}{index of the first element equal to v, or of the substring v in a string; -1 if absent}
\fn{(concat x ...)}{the arguments printed and joined into one string}
\fn{(split s sep)}{the pieces of s between occurrences of sep; (split s "") the characters}
\fn{(join L sep)}{the elements of L printed and joined with sep}
\fn{(format "text \{\} more \{\}" x y)}{each \{\} replaced by the next argument}
\fn{(upper s)  (lower s)  (trim s)}{case changes and whitespace trimming}
\fn{(chr n)}{the character with code n}
\fn{(ord s)}{the code of the first character of s}
\fn{(read path)}{the whole file as a string; a relative path that does not exist in the current directory is looked for next to the file being run (so a script finds its data from anywhere)}
\fn{(write path x)}{write x (printed) to a file, replacing it}
\fn{(append-file path x)}{write x (printed) at the end of a file}
\fn{(exists? path)}{does the file or directory exist?}
\fn{(input [prompt])}{a line read from the console, or nil at end of input}
\fn{(map x f)}{f applied to every element of a list (any results) or vector (numeric results)}
\fn{(filter x f)}{the elements for which f is true, as a list or vector like x}
\fn{(reduce x f init)}{fold: (f (f (f init x0) x1) x2) ...}
\fn{(assert cond [message ...])}{stop with an error unless cond is true}
\fn{(sign x)}{-1, 0 or 1}
\fn{(even? n)  (odd? n)}{}
\fn{(between? x lo hi)}{lo \textless{}= x \textless{}= hi}
\fn{(hypot a b)}{sqrt(a\^{}2 + b\^{}2)}
\fn{(gcd a b)  (lcm a b)}{on non-negative integers}
\fn{(lerp a b t)}{linear interpolation, t in [0, 1]}
\fn{(map-range x in-lo in-hi out-lo out-hi)}{re-map x from one range into another}
\fn{(deg-\textgreater{}rad d)  (rad-\textgreater{}deg r)}{}
\fn{(zeros n)}{vector of n zeros}
\fn{(ones n)}{vector of n ones}
\fn{(linspace a b n)}{n points from a to b inclusive}
\fn{(mean v)}{arithmetic mean}
\fn{(prod v)}{product of the elements}
\fn{(dot a b)}{inner product (sizes must match)}
\fn{(norm v)}{euclidean norm}
\fn{(cumsum v)}{running sum}
\fn{(stdev v)}{population standard deviation}
\fn{(normalize v)}{scale to [0, 1]; a constant vector becomes zeros}
\fn{(diff v)}{first differences, one element shorter}
\fn{(argmin v)  (argmax v)}{index of the smallest / largest element}
\fn{(median v)}{the middle value (mean of the two middle values for even length)}
\fn{(quantile v q)}{value below which a fraction q of the data lies (linear interpolation)}
\fn{(histogram v bins)}{counts per bin over [min v, max v]; returns (list counts edges)}
\fn{(interp1 xs ys xq)}{linear interpolation of the table (xs, ys) at the points xq; xs must be increasing; xq outside the table is clamped to the ends}
\fn{(clip v lo hi)}{clamp every element to [lo, hi]}
\fn{(fixed x d)}{round x to d decimal places}
\fn{(last x)}{last element}
\fn{(take x n)}{first n elements}
\fn{(drop x n)}{all but the first n elements}
\fn{(vec-\textgreater{}list v)}{the elements of a vector as a list}
\fn{(contains? x v)}{is v an element of x (or a substring, for strings)?}
\fn{(each x f)}{call f on every element, for side effects; returns nil}
\fn{(any? x f)}{does f hold for some element?}
\fn{(all? x f)}{does f hold for every element?}
\fn{(count x f)}{number of elements for which f holds}
\fn{(concat-list a b)}{a new list with the elements of a then those of b}
\fn{(flatten L)}{one level of nested lists removed}
\fn{(zip a b)}{list of two-element lists}
\fn{(unique L)}{elements in first-seen order, duplicates removed}
\fn{(sort-list L)}{sort a list of numbers or strings (insertion sort; fine for short lists)}
\fn{(min-of L)  (max-of L)}{smallest / largest element of a list of numbers}
\fn{(repeat s n)}{string s repeated n times}
\fn{(starts-with? s prefix)  (ends-with? s suffix)}{}
\fn{(replace s old new)}{every occurrence of old replaced by new}
\fn{(count-substr s pat)}{number of non-overlapping occurrences of pat}
\fn{(pad-left s w ch)  (pad-right s w ch)}{pad to width w with character ch}
\fn{(fmt-fixed x d)}{x as a string with exactly d decimals: (fmt-fixed pi 4) =\textgreater{} "3.1416"}
\fn{(fmt-pct x d)}{x as a percentage string: (fmt-pct 0.752 1) =\textgreater{} "75.2\%"}
\fn{(identity x)}{}
\fn{(compose f g)}{the function x -\textgreater{} (f (g x))}
\fn{(times n f)}{call f n times with 0 .. n-1}
\fn{(assert-near a b eps msg)}{numeric comparison within eps}
\fn{(assert-equal actual expected msg)}{structural comparison with a helpful message}
