% generated by tools/gendoc.py from the doc comments in src/; do not edit
\fn{(quote x)}{'x: x itself, unevaluated: code as data}
\fn{(do forms...)}{evaluate in order, return the last; what \{ \} builds}
\fn{(if cond then [else])}{cond is true unless it is 0, nil, empty, or a vector with a zero}
\fn{(while cond body)}{loop; (break) and (continue) inside it}
\fn{(for init cond step body)}{C-style loop: (for (var i 0) (\textless{} i 10) (var i (+ i 1)) \{ ... \})}
\fn{(var name value)}{define name in the current environment; a function's var is always local}
\fn{(set name value)}{assign to the nearest existing name, however far out; error if none}
\fn{(function name (params)}{body) define a function; (function (params) body) an anonymous one}
\fn{(return [value])}{leave the function; free in tail position}
\fn{(break)  (continue)}{leave or restart the innermost loop}
\fn{(try body catch e handler)}{run body; on an error bind its message to e and run handler}
\fn{(expr (a op b ...)}{) infix arithmetic: + - * / \% \textless{} \textgreater{} \textless{}= \textgreater{}= == != \&\& ||; ( ) groups, (f x) calls}
\fn{(eval form)}{evaluate a form (code built as data) in the global environment}
\fn{(apply f list)}{call f with the elements of list as arguments}
\fn{(+ a b ...)  (- a [b ...])  (* a b ...)  (/ a [b ...])}{arithmetic, elementwise with size-1 broadcasting; (- x) negates, (/ x) is the reciprocal, (+) is 0 and (*) is 1}
\fn{(\textless{} a b)  (\textgreater{} a b)  (\textless{}= a b)  (\textgreater{}= a b)}{comparisons: elementwise 1/0 on numbers, lexicographic on two strings}
\fn{(== a b)  (!= a b)}{equality: elementwise on numbers, structural on anything else}
\fn{(equal? a b)}{structural equality as a single 1/0, also for vectors and nested lists}
\fn{(sin x)  (cos x)  (tan x)  (asin x)  (acos x)  (atan x)  (sqrt x)  (exp x)  (log x)  (log2 x)  (log10 x)}{trigonometry and logs, elementwise}
\fn{(abs x)  (floor x)  (ceil x)  (round x)}{rounding and magnitude, elementwise}
\fn{(mod a b)  (pow a b)  (atan2 y x)}{remainder (sign follows fmod), power, two-argument arctangent; elementwise}
\fn{(not x)  (and a b ...)  (or a b ...)}{logic on truth values; and/or return 1 or 0 and evaluate every argument}
\fn{(min v)  (max v)}{smallest or largest element of one vector; with several arguments, elementwise over them}
\fn{(list x ...)}{a list of anything}
\fn{(cons x L)}{a new list with x in front; (append L x ...) a new list with x ... at the end}
\fn{(push L x)}{append in place, returning the list; (pop L) remove and return the last element}
\fn{(append L x ...)}{a new list with x ... added at the end}
\fn{(pop L)}{remove and return the last element of a list, in place}
\fn{(length x)  (head x)  (tail x)  (empty? x)}{on a list, a vector or a string}
\fn{(getidx x k)}{element k of a list, vector or string; negative k counts from the end}
\fn{(setidx x k v)}{set element k of a list or vector in place}
\fn{(vec x ...)}{a vector from numbers, vectors and lists of numbers, concatenated; a scalar is a vector of size 1}
\fn{(sum v)}{sum of the elements (0 for an empty vector)}
\fn{(print x ...)}{print the arguments separated by spaces, then a newline}
\fn{(exit [code])}{end the program}
\fn{(type x)}{"scalar" "vec" "string" "symbol" "list" "function" "nil" or "opaque:tag"}
\fn{(str x)}{x printed as a string}
\fn{(sym s)}{a symbol from a string; (num s) a number from a string}
\fn{(copy x)}{a shallow copy: a new list or vector with the same elements}
\fn{(error x ...)}{raise an error with the arguments as message}
\fn{(clock)}{seconds from a monotonic clock, for timing}
\fn{(load "file.mu")}{run a file once, searched next to the loading file, in the current directory, in MUSIL\_PATH and in \textasciitilde{}/.musil; (find-file "name") the path load would use, or nil}
\fn{(defined? 'name)}{is name bound?}
\fn{(vars)}{the global names, sorted}
\fn{(help name)}{print the documentation of a builtin or library function (from help.txt)}
\fn{(num s)}{the number written in a string (or a number unchanged)}
\fn{(find-file "name")}{the path load would use for name, searched the same way, or nil}
