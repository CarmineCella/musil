% generated by tools/gendoc.py from the doc comments in src/; do not edit
\fn{(save-png fig path [w h])}{render a figure to a PNG file (900 x 560 by default)}
\fn{(show fig [w h])}{show a figure: a window in the CLI (Esc closes; none when MUSIL\_NOSHOW is set), a panel in the Listener}
\fn{(figure title)}{an empty figure}
\fn{(fig-title fig)  (fig-layers fig)  (fig-options fig)}{the three parts of a figure}
\fn{(fig-layers fig)}{the layers}
\fn{(fig-options fig)}{the options}
\fn{(add-line fig x y label)}{x may be nil to use 0, 1, 2, ...}
\fn{(add-scatter fig x y label)}{points; (add-bars fig x y label) bars}
\fn{(add-bars fig x y label)}{bars}
\fn{(add-image fig M)}{a matrix as a heat map, row 0 at the bottom}
\fn{(add-surface fig M)}{a matrix as a 3D surface (a figure with a surface shows only that)}
\fn{(add-layer fig layer)}{add a raw layer (list kind data ...)}
\fn{(x-or-index x y)}{x, or 0 1 2 ... when x is nil}
\fn{(set-option fig key value)}{keys: "xlabel" "ylabel" "xmin" "xmax" "ymin" "ymax"}
\fn{(set-labels fig xlabel ylabel)}{axis labels; (set-xrange fig lo hi) (set-yrange fig lo hi) axis limits}
\fn{(set-xrange fig lo hi)}{x-axis limits}
\fn{(set-yrange fig lo hi)}{y-axis limits}
\fn{(plot y)}{or (plot-xy x y)      a line}
\fn{(plot-xy x y)}{a line of y against x}
\fn{(plot-lines ys labels)}{several lines sharing the index axis}
\fn{(scatter x y)  (bars y)  (image M)  (surface M)}{build and show a one-layer figure}
\fn{(bars y)}{build and show a bar chart}
\fn{(image M)}{build and show a heat map}
\fn{(surface M)}{build and show a 3D surface}
\fn{(waveform x sr)}{a signal against time in seconds}
\fn{(spectrum x sr)}{magnitude spectrum in dB against frequency}
\fn{(spectrogram x sr n hop)}{STFT magnitudes in dB as an image: time along x, frequency along y}
\fn{(scatter-clusters X labels k)}{the rows of an n x 2 matrix coloured by cluster}
\fn{(histogram-figure v bins)}{a bar chart of (histogram v bins)}
