% generated by tools/gendoc.py from the doc comments in src/; do not edit
\fn{(mat-mul A B ...)}{matrix product, left to right}
\fn{(transpose M)}{}
\fn{(det M)}{}
\fn{(rank M)}{}
\fn{(inv M)}{by Gauss-Jordan on [M | I]}
\fn{(solve A b)}{=\textgreater{} x with A x = b}
\fn{(median-filter v order)}{the running median of width order}
\fn{(eig-sym M)}{=\textgreater{} (list eigenvalues eigenvectors) of a symmetric matrix by Jacobi rotations;}
\fn{(kmeans M k)}{=\textgreater{} (list labels centroids): labels is a vector of cluster indices, centroids a k x d matrix}
\fn{(knn training k queries)}{=\textgreater{} list of predicted labels.}
\fn{(list-\textgreater{}mat L)}{a matrix from a list of lists of numbers}
\fn{(mat-fill r c v)}{r x c matrix filled with v}
\fn{(mat-zeros r c)  (mat-ones r c)}{matrices of zeros or ones}
\fn{(mat-ones r c)}{a matrix of ones}
\fn{(mat-rand r c)}{r x c matrix of values in [-1, 1]}
\fn{(eye n)}{identity; (diag v) diagonal matrix from a vector}
\fn{(diag v)}{a square matrix with v on the diagonal}
\fn{(mat-diag M)}{the diagonal of a matrix as a vector}
\fn{(nrows M)  (ncols M)}{the number of rows or columns}
\fn{(ncols M)}{the number of columns}
\fn{(mat-shape M)}{(list rows cols)}
\fn{(mat-get M i j)}{element; (mat-set M i j v) in place}
\fn{(mat-set M i j v)}{set an element in place}
\fn{(mat-row M i)}{row i as a vector; (mat-col M j) column j as a vector}
\fn{(mat-col M j)}{column j as a vector}
\fn{(get-rows M from n)}{n rows starting at from, as a matrix}
\fn{(get-cols M from n)}{n columns starting at from, as a matrix}
\fn{(mat-vec A x)}{A x as a vector; (vec-mat x A) x A as a vector}
\fn{(vec-mat x A)}{x A as a vector}
\fn{(outer u v)}{the matrix u v', rows u[i] * v}
\fn{(trace M)}{sum of the diagonal}
\fn{(mat-add A B)  (mat-sub A B)  (hadamard A B)}{elementwise sum, difference and product of two matrices}
\fn{(mat-sub A B)}{elementwise difference}
\fn{(hadamard A B)}{elementwise product}
\fn{(mat-scale M s)  (mat-shift M s)}{every element times s, or plus s}
\fn{(mat-shift M s)}{every element plus s}
\fn{(mat-map M f)}{f applied to every element}
\fn{(mat-round M d)}{every element rounded to d decimals (for printing)}
\fn{(vstack A B)}{rows of A then rows of B}
\fn{(hstack A B)}{each row of A followed by the row of B}
\fn{(col-stack vectors)}{a matrix whose columns are the given vectors: (col-stack (list x y))}
\fn{(lp-norm v p)}{(sum |v|\^{}p)\^{}(1/p); p = 2 is the euclidean norm}
\fn{(dist x y)}{euclidean distance; (dist-p x y p) with another p}
\fn{(dist-p x y p)}{distance with another p: 1 for the city-block distance}
\fn{(dist-matrix M)}{pairwise euclidean distances between the rows of M}
\fn{(nearest M x)}{index of the row of M closest to x}
\fn{(mat-sum M axis)}{}
\fn{(mat-mean M axis)}{}
\fn{(mat-std M axis)}{population standard deviation}
\fn{(zscore M)}{columns standardised to mean 0, std 1 (constant columns become 0)}
\fn{(standardize M)}{the same as zscore}
\fn{(center M)}{each column minus its mean}
\fn{(cov M)}{sample covariance of the columns (rows are observations)}
\fn{(corr M)}{correlation of the columns; constant columns give 0}
\fn{(linefit x y)}{(slope intercept) of the least-squares line through the points}
\fn{(mat-str M d)}{one line per row, d decimals, columns aligned}
\fn{(mat-print M)}{print with 3 decimals; (mat-print-with M d) chooses the decimals}
\fn{(mat-print-with M d)}{print with d decimals}
\fn{(linreg-fit X y)}{coefficients b with y \textasciitilde{} X b, by the normal equations; y is a vector}
\fn{(linreg-predict X b)}{X b as a vector}
\fn{(linreg-residuals X y b)}{}
\fn{(add-intercept X)}{a column of ones in front of X, for a model with a constant term}
\fn{(pca X)}{=\textgreater{} d x (d+1) matrix: row k is the k-th principal direction followed by its eigenvalue, largest first}
\fn{(pca-directions P)}{the directions of a pca result as a d x d matrix}
\fn{(pca-eigenvalues P)}{its eigenvalues as a vector}
\fn{(pca-scores X k)}{X projected on the first k principal directions, n x k}
\fn{(pca-explained P)}{fraction of variance per component}
\fn{(bpf start segments)}{piecewise linear function as one vector; segments is a list of (list length end): (bpf 0 (list (list 4 1) (list 4 0))) rises then falls. Each segment's end value is excluded (it starts the next one).}
\fn{(kmeans-labels result)  (kmeans-centroids result)}{the two parts of a kmeans result}
\fn{(kmeans-centroids result)}{the centroids of a kmeans result, k x d}
\fn{(cluster-sizes labels k)}{how many points fell in each of the k clusters}
\fn{(knn-model training k)}{a model: (list training k); training is a list of (list features label)}
\fn{(knn-predict model queries)}{labels for a list of feature vectors}
\fn{(knn-predict-one model q)}{}
\fn{(knn-test model samples)}{predictions for samples given as (list features label)}
\fn{(accuracy predictions samples)}{fraction of predictions equal to the samples' labels}
\fn{(train-test-split samples fraction)}{(list training test), first part of the list}
\fn{(shuffle L)}{a new list in random order (Fisher-Yates)}
